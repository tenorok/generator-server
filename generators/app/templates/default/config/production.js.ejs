// Конфиг для `staging` и `production`.

module.exports = {
<% if (mongodb !== 'no') { -%>
    mongodb: {
        host: process.env.MONGO_PORT_27017_TCP_ADDR,
        port: process.env.MONGO_PORT_27017_TCP_PORT,
        db: process.env.MONGO_DB_NAME,
    },
<% } -%>
    log: {
        src: false,
        level: 'warn',
    },
};
