module.exports = {
    apps: [{
        name: '<%= project %>',
        script: 'dist/app/src/index.js',
        max_memory_restart: '256M',
<% if (server) { -%>
        wait_ready: true,
        kill_timeout: 3000,
<% } -%>
        env_development: {
            watch: ['dist'],
            watch_options: {
                usePolling: true, // Для работы в докер-контейнере.
            },
        },
    }],
};
